---
packages:
  - !include common/devices/esp32-s3.yaml
  - !include common/base_package.yaml

substitutions:
  api_key: !secret api_encryption_key
  ota_password: !secret ota_password
  base_wifi_ssid: !secret wifi_ssid
  base_wifi_password: !secret wifi_password
  ap_wifi_password: !secret ap_password
